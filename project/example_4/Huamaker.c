#include "Huamaker.h"

void makeHua(unsigned int uint_pCount)
{

  // let's hua :)
  for (unsigned int i = 0; i < uint_pCount; i++)
  {
  // call printer
  print_out("hua ");
  }

  // call printer
  print_out("!!!");
}
// we don't have a printer
